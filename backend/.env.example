# Optional — copy to .env and adjust
DATABASE_URL=sqlite:///./signaldesk.db
LOG_LEVEL=INFO

# Production example (requires psycopg driver):
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/signaldesk
